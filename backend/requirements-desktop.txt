pywebview>=6.2
pyinstaller>=6.10
# Windows production uses pywebview's WebView2/ pythonnet backend. Qt remains a dependency for
# the Linux production renderer and separately-built Windows diagnostic packages.
PyQt6>=6.7; sys_platform != "darwin"
PyQt6-WebEngine>=6.7; sys_platform != "darwin"
QtPy>=2.4; sys_platform != "darwin"
